source 'https://rubygems.org'

gemspec

group :development do
  gem 'bump'
  gem 'rake', '~> 13.0'
end

group :test do
  gem 'test-unit', '~> 3.6'
end

group :optional do
  gem 'redis', '~> 5.0' # Optional: for Redis-based storage backend
end